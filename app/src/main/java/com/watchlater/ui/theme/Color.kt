package com.watchlater.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Core Surface Scale ────────────────────────────────────────────────────
val Background        = Color(0xFF0A0A0F)   // Deepest base
val SurfaceContainer  = Color(0xFF111118)   // Card / sheet background
val SurfaceElevated   = Color(0xFF1A1A24)   // Elevated card
val SurfaceBorder     = Color(0xFF252535)   // Dividers / borders
val SurfaceHighlight  = Color(0xFF2A2A3E)   // Hover / pressed states

// ─── Text ──────────────────────────────────────────────────────────────────
val TextPrimary    = Color(0xFFF0F0F8)
val TextSecondary  = Color(0xFF9898B8)
val TextTertiary   = Color(0xFF5A5A7A)
val TextDisabled   = Color(0xFF3A3A5A)

// ─── Accent ────────────────────────────────────────────────────────────────
val AccentBlue     = Color(0xFF5B6EF5)   // Primary CTA
val AccentBlueDim  = Color(0xFF3D4FBF)
val AccentPurple   = Color(0xFF8B5CF6)   // Secondary accent
val AccentGlow     = Color(0x295B6EF5)   // Soft glow overlays

// ─── Semantic ──────────────────────────────────────────────────────────────
val SemanticSuccess  = Color(0xFF34D399)
val SemanticWarning  = Color(0xFFFBBF24)
val SemanticError    = Color(0xFFF87171)
val SemanticInfo     = Color(0xFF60A5FA)

// ─── Type Badges ───────────────────────────────────────────────────────────
val MovieBadgeBg   = Color(0xFF1E2A4A)
val MovieBadgeFg   = Color(0xFF93B4FF)
val SeriesBadgeBg  = Color(0xFF2A1E4A)
val SeriesBadgeFg  = Color(0xFFB593FF)

// ─── Star Rating ───────────────────────────────────────────────────────────
val StarFilled  = Color(0xFFFBBF24)
val StarEmpty   = Color(0xFF2A2A3E)

// ─── Watched Indicator ─────────────────────────────────────────────────────
val WatchedGreen    = Color(0xFF34D399)
val WatchedGreenBg  = Color(0xFF0D2620)
