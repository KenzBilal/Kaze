package com.watchlater.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Core Surface Scale ────────────────────────────────────────────────────
val Background        = Color(0xFF000000)   // Pure black base
val SurfaceContainer  = Color(0xFF0F0F0F)   // Card / sheet background
val SurfaceElevated   = Color(0xFF1A1A1A)   // Elevated card
val SurfaceBorder     = Color(0xFF2A2A2A)   // Dividers / borders
val SurfaceHighlight  = Color(0xFF333333)   // Hover / pressed states

// ─── Text ──────────────────────────────────────────────────────────────────
val TextPrimary    = Color(0xFFFFFFFF)
val TextSecondary  = Color(0xFFA0A0A0)
val TextTertiary   = Color(0xFF606060)
val TextDisabled   = Color(0xFF3D3D3D)

// ─── Accent — monochrome only ───────────────────────────────────────────────
val AccentBlue     = Color(0xFFFFFFFF)   // White — primary CTA
val AccentBlueDim  = Color(0xFFAAAAAA)   // Mid grey — secondary CTA
val AccentPurple   = Color(0xFFCCCCCC)   // Light grey — secondary accent
val AccentGlow     = Color(0x22FFFFFF)   // Subtle white glow overlays

// ─── Semantic ──────────────────────────────────────────────────────────────
val SemanticSuccess  = Color(0xFFFFFFFF)   // White — success
val SemanticWarning  = Color(0xFFAAAAAA)   // Mid grey — warning
val SemanticError    = Color(0xFFE0E0E0)   // Light grey — error
val SemanticInfo     = Color(0xFF888888)   // Dark grey — info

// ─── Type Badges ───────────────────────────────────────────────────────────
val MovieBadgeBg   = Color(0xFF1E1E1E)
val MovieBadgeFg   = Color(0xFFCCCCCC)
val SeriesBadgeBg  = Color(0xFF2A2A2A)
val SeriesBadgeFg  = Color(0xFFE0E0E0)

// ─── Star Rating ───────────────────────────────────────────────────────────
val StarFilled  = Color(0xFFFFFFFF)
val StarEmpty   = Color(0xFF2A2A2A)

// ─── Watched Indicator ─────────────────────────────────────────────────────
val WatchedGreen    = Color(0xFFFFFFFF)   // White for watched state
val WatchedGreenBg  = Color(0xFF1A1A1A)
